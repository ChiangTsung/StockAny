interface:
  display_name: "StockAny"
  short_description: "US + A-share research and charter workflow"
  default_prompt: "Use $stockany and prefer ./stockany topic turn prepare/commit as the main workflow to maintain topic reports, issuer snapshots, evaluation preferences, and a progressive investment charter."

policy:
  allow_implicit_invocation: true
